{
  "name": "chemlab-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "the phone client for the chemlab warehouse: 118 elements, 581 substances, 424 reactions, 380 ion pairs, 9410 element-pair rows",
  "scripts": {
    "sync": "node scripts/sync-data.mjs",
    "dev": "vite",
    "prebuild": "npm run sync --silent && npm run icons --silent",
    "build": "vite build && node scripts/prune-dist.mjs && node scripts/gen-sw.mjs",
    "serve": "node scripts/serve.mjs",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test --silent && npm run build --silent",
    "test": "vitest run",
    "icons": "node scripts/make-icons.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
